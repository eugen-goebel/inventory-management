services:
  backend:
    build: ./backend
    environment:
      DATABASE_URL: sqlite:////data/inventory.db
    volumes:
      - inventory-data:/data
    expose:
      - "8000"

  frontend:
    build: ./frontend
    depends_on:
      - backend
    ports:
      - "8080:80"

volumes:
  inventory-data:
